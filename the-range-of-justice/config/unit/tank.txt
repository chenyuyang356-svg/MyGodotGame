#########################################
# 坦克单位配置文件 (Marine)
#########################################

unit_name = Tank
unit_name_key = KEY_UNIT_TANK
# --- Survival (生存) ---
health_max       = 75.0
health_regen     = 0.5
shield_max       = 0.0
shield_regen     = 0.0
# 枚举可选: Light, Heavy, Building, Hero
armor_type       = Light

# --- Attack (攻击) ---
attack_damage    = 6.0
attack_range     = 1200
attack_interval  = 0.86
# 枚举可选: Physical, Magic, Siege
attack_type      = Physical
projectile_speed = 800.0
splash_radius    = 0.0
# 枚举可选: Closest, Lowest_hp, Highest_value
target_priority  = Closest
# 挂载武器 1: 机枪 (调用 MarineBullet 子弹，伤害6，射程250，0.15秒开一枪)
weapon = Missile, 10.0, 300.0, 0.3, 500.0, 4.0
# 格式: weapon_mount = [武器名称],[相对单位中心的本地偏移X], [本地偏移Y]
weapon_mount = HeavyCannon, 0.0, 0.0

# --- Movement (移动与碰撞) ---
mass             = 1.0
move_speed       = 150.0
acceleration     = 100.0
turn_speed       = 1.5
turn_acceleration= 2.0
# [关键] 碰撞半径
collision_radius = 48.0
# 枚举可选: Ground, Air, Sea, Hover
base_height = 0.0
move_type        = Ground

# --- Vision (视野) ---
sight_range      = 700.0
aggro_range      = 3000.0

# --- Economy (经济) ---
cost             = 500
build_time       = 15.0

# --- Misc (其他/标签) ---
# 支持逗号分隔的多个标签
unit_tags        = Biological

# 渲染基础
texture_path = res://asset/unit/地面单位02.png
h_frames = 8
v_frames = 3

# 状态机动画定义
idle_row = 0
idle_frames = 1
move_row = 1
move_frames = 1
anim_fps = 10

# 粒子
# 定义单位每移动多少距离（像素）产生一次粒子。
# -1.0: 使用默认逻辑（陆地为 1.2 * 半径，海洋为 0.5 * 半径）
emit_threshold = 25.0
# 语法: effect_point = 粒子类型, X偏移, Y偏移, 缩放覆盖, 寿命覆盖, 是否波纹
effect_point = Dust, -20.0, -40.0, 2.0, -1, false
effect_point = Dust,  20.0, -40.0, 2.0, -1, false
